package model;

// Represents a player's game board and the ships on it
public class GameBoard {
    private static final int SIZE = 10;
    private Ship[][] grid = new Ship[SIZE][SIZE];
    // Could also have a grid for shots received
}
